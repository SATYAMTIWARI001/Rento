import React from "react";
import { createRoot } from "react-dom/client";
import Rento from "./Rento.jsx";

createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <Rento />
  </React.StrictMode>
);